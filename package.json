{
  "name": "mini-webapp",
  "version": "1.0.0",
  "description": "Flask web application with PyTorch model",
  "main": "app.py",
  "scripts": {
    "start": "python app.py",
    "build": "pip install -r requirements.txt"
  },
  "dependencies": {},
  "devDependencies": {
    "@netlify/plugin-python": "^1.2.0"
  }
}
